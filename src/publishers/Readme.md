### Publishers 


I used
```
nest g resource publishers
```

to generate this.

This command not only generates all the NestJS building blocks (module, service, controller classes) but also an entity class, DTO classes as well as the testing (.spec) files.